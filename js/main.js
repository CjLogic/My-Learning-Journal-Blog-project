// Main page JavaScript
document.addEventListener('DOMContentLoaded', () => {
    // The blog grid is already populated in the HTML
    // This file can be used for additional functionality
    // such as filtering, sorting, or loading more posts
});